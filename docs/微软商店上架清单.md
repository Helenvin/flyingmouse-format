# Microsoft Store 上架清单

更新时间：2026-09-13

## 状态边界

当前源码和本地交付为 **0.7.1 修复候选**。Windows x64 APPX 已生成，包版本 `0.7.1.0`，本地包 CRC、身份和 ASAR 校验通过；未签名，未上传 Partner Center，未完成商店安装、认证或公开发布验收。现役包信息、验证结果和远程 CI 前置项统一见 [0.7.1 修复候选](REPAIR-0.7.1.md)。

2026-09-13 本轮没有回读 Partner Center，既有商店版本和审核状态仍待现场核实；下面的旧提交记录不能代表当前状态。

## 历史提交记录

- **2026-08-10**：v0.3.3 上传验证通过并提交认证，Submission 2 / `1152921505701615843`；当时最后回读为 `Pre-processing in progress` / `In certification`，未取得认证或发布完成证据。
- **2026-08-13**：用户确认已上传 v0.4.1 / `0.4.1.0` x64 APPX；认证与发布结果未在本轮复核。

**注意（图标两套资源）**：商店页面展示图标来自 Store listing 素材，与包内 assets 独立。若商店仍显示橙色闪电，除包版本外还需确认 Store listing 的 logo 素材已同步为鼠鼠。

## 已记录的产品身份

| 项目 | 值 |
|---|---|
| 产品名称 | FlyingMouse Format |
| 开发者 | 牢蜂（LaoFeng） |
| Publisher | `CN=56D7A4A1-B653-4162-A5E2-7D9B3F3754AD` |
| Identity name | `488B6338.354574AC174AD` |
| 产品 ID | `9NJKN37CR6HJ` |
| 支持页面 | `https://github.com/LaoFeng-mouse/flyingmouse-format` |
| 隐私政策 | `https://laofeng-mouse.github.io/flyingmouse-format/docs/privacy-policy.html` |

本地 `package.json` 和 0.7.1 APPX 使用上述 Identity / Publisher。产品 ID、支持页面和隐私政策地址沿用历史记录，本轮未现场验证 Partner Center 或链接可访问性。每次提交仍须重新核对预留名称和 Publisher display name；不要依据乱码旧文档直接填写。

## 0.7.1 提交前检查

- [x] 构建 Windows x64 APPX `0.7.1.0`，验证本地包 CRC、身份、版本和 ASAR。
- [x] 整体重建的 Windows 程序已完成真实转换验证；详见修复候选记录。
- [ ] 同步新版原生结构引擎的远程资产与 CI 清单，运行远程门禁。
- [ ] 验收正式商店安装、启动和转换；本地 Store 路径模拟不替代该项。
- [ ] 重新截取中文与英文商店截图，尺寸符合 Partner Center 当前要求。
- [ ] 截取 PDF → Excel 智能表格提示，并确保文案没有承诺扫描件或复杂表格 100% 准确。
- [ ] 商店图标、截图和文字不得使用旧橙色闪电/中性界面。
- [ ] 上传前现场核对包身份与 Partner Center 预留名称完全一致。
- [ ] 取得本次 Partner Center 包上传验证结果。
- [ ] 检查隐私政策与支持链接可访问。
- [ ] 复核现有免费、市场和年龄分级信息后提交。
- [ ] 记录本次 submission ID、提交时间和当前阶段。
- [ ] 认证完成后现场回读最终结果、发布时间和商店下载链接。

## 商店文案边界

商店公开文案聚焦本地离线文件转换、OCR、批量处理、格式与保存路径记忆以及中英文界面。不要在商店列表中宣传 NCM、KGG 或绕过 DRM；具体格式兼容说明保留在项目 README。

## 签名说明

GitHub NSIS 安装包当前未签名。Microsoft Store 会对通过商店分发的包执行其签名流程，但提交包本身仍须满足 Partner Center 当时的格式、身份和认证要求。

## 历史安装故障与本轮启动修复

0.7.1 将 Office 准备移到后台并复用按引擎内容标识的缓存，使界面先显示；Office 准备失败时保留界面与诊断。该修复已完成本地 Store 路径模拟，不能据此宣称覆盖所有用户启动或安装错误。

旧 v0.2 商店包在“检查依赖项”阶段出现 `0x8007041D` 时，错误含义是 Windows 服务请求超时，不等同于缺少 VC++、.NET 或 APPX 框架依赖。已检查的 v0.2.1 Manifest 仅声明 `Windows.Desktop` TargetDeviceFamily，没有 Framework `PackageDependency`。优先让用户重启电脑，并检查 Microsoft Store Install Service、Windows Update、BITS 与 Delivery Optimization 服务；再执行商店“修复/重置”、`wsreset.exe` 和 Windows 更新。若仍失败，应收集 Windows 版本、商店诊断信息和事件查看器日志，不要通过随意补装依赖包掩盖服务故障。
